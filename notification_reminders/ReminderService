// Source code is decompiled from a .class file using FernFlower decompiler.
package notification_reminders;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class ReminderService {
   private Notifiable notificationMethod;

   public ReminderService(Notifiable notificationMethod) {
      this.notificationMethod = notificationMethod;
   }

   public void sendAppointmentReminder(String patientId, LocalDateTime appointmentTime) {
      String formattedTime = appointmentTime.format(DateTimeFormatter.ofPattern("MMM dd, yyyy hh:mm a"));
      String message = "Reminder: You have an appointment scheduled for " + formattedTime;
      this.notificationMethod.sendNotification(patientId, message);
   }

   public void sendMedicationReminder(String patientId, String medication, String dosage) {
      String message = "Reminder: Take " + dosage + " of " + medication;
      this.notificationMethod.sendNotification(patientId, message);
   }

   public void setNotificationMethod(Notifiable notificationMethod) {
      this.notificationMethod = notificationMethod;
   }
}
